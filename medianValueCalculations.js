import { MeanValueCalculations } from './meanValueCalculations'

class MedianValueCalculations extends MeanValueCalculations {
  /**
   * Method that calculates the medain value from an array.
   * Uses the private method #checkArraysInput, inherited from MeanValueCalculations class and used for validation of the aray being used.
   */

}
